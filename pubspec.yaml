name: sample_app_uses_uikit
description: new app uses ui-kit 
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=2.18.0 <3.0.0'
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  # app_ui_kit:
  #   git:
  #     url: https://github.com/pedramRoosta/simple_ui_kit.git
  #     ref: master


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
flutter:
  uses-material-design: true
